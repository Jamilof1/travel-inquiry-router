import{describe,expect,it}from'vitest';import{routeInquiry,type Inquiry}from'./route';const q:Inquiry={name:'Mira',email:'m@example.com',destination:'Japan',origin:'Berlin',depart:'2030-04-02',nights:12,travelers:2,tripType:'Luxury tailor-made',budget:'€12k+',notes:''};describe('travel routing',()=>{it('routes Japan to APAC private travel',()=>{const r=routeInquiry(q);expect(r.region).toBe('Asia Pacific');expect(r.workflow).toBe('Private travel design');});it('identifies missing contact fields',()=>expect(routeInquiry({...q,email:''}).requirements).toContain('Contact email'));});
